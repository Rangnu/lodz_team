'use client';

export default function LoginPage() {
  return (
    <div className="min-h-screen bg-white flex items-center justify-center">
      <div className="w-full max-w-md p-8 border rounded"> 
        <h1 className="text-xl font-bold mb-4">Login (placeholder)</h1>
        <p className="text-sm text-gray-600">Use the main login form at / (temporary)</p>
      </div>
    </div>
  );
}
